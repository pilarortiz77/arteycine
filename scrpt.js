// script.js
// Descripción: Archivo JavaScript para funcionalidades interactivas del sitio.

// NOTA: Para este proyecto inicial, muchas funcionalidades visuales y de UI
// se manejan directamente con clases de utilidad de TailwindCSS.
// Este archivo está listo para añadir lógica JavaScript más compleja
// en el futuro, como:
// - Carruseles de imágenes dinámicos
// - Filtrado avanzado de galería (más allá de enlaces simples)
// - Validación de formularios en tiempo real (más allá de HTML5)
// - Animaciones con JavaScript
// - Integraciones con APIs externas (ej. para mapas más interactivos, etc.)

// Ejemplo de una función JavaScript básica que podría ser útil:
// (Descomenta y adapta si la necesitas)

/*
document.addEventListener('DOMContentLoaded', () => {
    // Código a ejecutar una vez que el DOM esté completamente cargado.
    console.log('El DOM ha sido cargado completamente.');

    // Ejemplo: Añadir un listener a un botón para mostrar un mensaje
    const miBoton = document.getElementById('miBoton');
    if (miBoton) {
        miBoton.addEventListener('click', () => {
            alert('¡Botón clicado!');
        });
    }

    // Ejemplo: Función para un "scroll to top"
    const scrollToTopBtn = document.getElementById('scrollToTopBtn');
    if (scrollToTopBtn) {
        window.addEventListener('scroll', () => {
            if (window.scrollY > 300) { // Mostrar botón después de 300px de scroll
                scrollToTopBtn.style.display = 'block';
            } else {
                scrollToTopBtn.style.display = 'none';
            }
        });

        scrollToTopBtn.addEventListener('click', () => {
            window.scrollTo({
                top: 0,
                behavior: 'smooth'
            });
        });
    }
});
*/

// Asegúrate de enlazar este script al final de la etiqueta <body> de cada archivo HTML
// donde necesites que se ejecute. Por ejemplo:
// <script src="./script.js"></script>
// (Justo antes de </body>)
